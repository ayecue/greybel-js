{
    "dump_lib": {
        "example": [
            "meta = include_lib(\"/lib/metaxploit.so\")",
            "net = meta.net_use(\"88.231.19.24\", 22)",
            "libssh = net.dump_lib",
            "print(libssh.lib_name + \" v\" + libssh.version)"
        ],
        "description": "Returns the MetaLib object of the library behind the service to which the NetSession belongs.",
        "returns": ["metaLib"]
    }
}